{
  "name": "go.server",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/web",
    "packages/admin-console"
  ],
  "scripts": {
    "bundle": "redocly bundle openapi/openapi.yaml -o openapi/openapi-bundled.yaml",
    "gen:api": "orval --config openapi/orval.config.ts",
    "dev:web": "bun --filter web dev",
    "dev:admin": "bun --filter admin-console dev",
    "build:web": "bun --filter web build",
    "build:admin": "bun --filter admin-console build",
    "typecheck": "bun --filter '*' typecheck"
  },
  "devDependencies": {
    "@redocly/cli": "^2.35.1",
    "orval": "^7.13.2"
  }
}
